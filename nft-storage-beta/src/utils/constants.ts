export type Bindings = {
  Bindings: {
    R2_BUCKET: R2Bucket;
    DEDICATED_GATEWAY: string;
    DEDICATED_BACKUP_GATEWAY: string;
    NFTSTORAGE_IPFS: string;
    CF_IMAGE_ID: string;
    CF_IMAGE_ACCOUNT: string;

    // secrets
    NFT_STORAGE_TOKEN: string;
    IMAGE_API_TOKEN: string;
  };
};
